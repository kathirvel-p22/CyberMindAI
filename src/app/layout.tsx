import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CyberMind AI — Autonomous Cyber Defense Operating System",
  description:
    "AICDOS: Continuous monitoring, AI-driven threat prediction, MITRE ATT&CK mapping, autonomous response, and organizational cyber memory for Critical National Infrastructure.",
  keywords: [
    "CyberMind AI",
    "SOC",
    "AICDOS",
    "MITRE ATT&CK",
    "Threat Intelligence",
    "Autonomous Defense",
  ],
  authors: [{ name: "CyberMind AI" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
